apiVersion: v1
kind: Service
metadata:
  name: mcart-product-service
  namespace: mcart
spec:
  selector:
    app: mcart-product-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080